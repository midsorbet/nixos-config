diff --git a/relay/herdr_relay.py b/relay/herdr_relay.py
index d528a94..4307471 100644
--- a/relay/herdr_relay.py
+++ b/relay/herdr_relay.py
@@ -4,7 +4,7 @@
 # dependencies = ["websockets>=14.0", "zeroconf>=0.80.0", "pywebpush>=2.0.0", "py-vapid>=1.9.0"]
 # ///
 """herdr-remote relay — polls herdr, accepts push events (HTTP POST + WebSocket + UDP), broadcasts to clients."""
-import asyncio, json, logging, os, re, shutil, signal, socket, subprocess, time
+import asyncio, base64, json, logging, os, re, shutil, signal, socket, subprocess, time
 
 from agent_state import complete_agent_update_message
 
@@ -17,6 +17,13 @@ from websockets.exceptions import ConnectionClosedError, ConnectionClosedOK
 from logging.handlers import RotatingFileHandler
 import sys
 
+
+def env_bool(name: str, default: bool) -> bool:
+    value = os.environ.get(name)
+    if value is None:
+        return default
+    return value.strip().lower() in {"1", "true", "yes", "on"}
+
 def _get_log_dir():
     if sys.platform == "darwin":
         return os.path.expanduser("~/Library/Logs/herdr-remote")
@@ -42,9 +49,15 @@ log.addHandler(_console_handler)
 logging.getLogger("websockets").setLevel(logging.WARNING)
 
 HERDR = os.environ.get("HERDR_BIN") or shutil.which("herdr") or "/opt/homebrew/bin/herdr"
+BIND_ADDRESS = os.environ.get("HERDR_RELAY_BIND", "127.0.0.1")
 WS_PORT = int(os.environ.get("HERDR_RELAY_PORT", "8375"))
 POLL_INTERVAL = 2
-AUTH_TOKEN = os.environ.get("HERDR_RELAY_TOKEN", "")  # Optional: shared secret for relay auth
+READ_ONLY = env_bool("HERDR_RELAY_READ_ONLY", True)
+REQUIRE_ACCESS_JWT = env_bool("HERDR_REQUIRE_ACCESS_JWT", True)
+ALLOWED_ORIGIN = os.environ.get("HERDR_ALLOWED_ORIGIN", "")
+MAX_CLIENTS = int(os.environ.get("HERDR_MAX_CLIENTS", "2"))
+MAX_CONNECTION_SECONDS = int(os.environ.get("HERDR_MAX_CONNECTION_SECONDS", "1800"))
+MDNS_ENABLED = env_bool("HERDR_MDNS", False)
 
 # VAPID Web Push
 VAPID_PUBLIC_KEY = os.environ.get("HERDR_VAPID_PUBLIC", "")
@@ -77,6 +90,15 @@ SAFE_RESPONSES = {"y", "n", "a", "yes", "no", "trust", "yes, single permission",
 SAFE_KEYS = {"y", "n", "a", "Enter", "Tab", "Escape", "C-c", "Up", "Down", "Left", "Right", "BSpace"} | {
     str(number) for number in range(10)
 }
+MUTATING_MESSAGE_TYPES = {
+    "agent_event",
+    "create_tab",
+    "push_subscribe",
+    "push_unsubscribe",
+    "respond",
+    "send_keys",
+    "send_text",
+}
 
 # --- Audit logging ---
 _audit_handler = RotatingFileHandler(AUDIT_FILE, maxBytes=5 * 1024 * 1024, backupCount=3)
@@ -325,105 +347,77 @@ async def event_push():
             await broadcast(update)
 
 
-async def process_request(connection, request):
-    """Handle HTTP POST on the same port as WebSocket."""
+def request_header(request, name: str) -> str:
+    return request.headers.get(name, "") if request and request.headers else ""
+
+
+def http_response(status: int, reason: str, body: bytes, content_type: str):
     from websockets.http11 import Response
     from websockets.datastructures import Headers
 
-    # Token auth (if configured)
-    if AUTH_TOKEN:
-        token = None
-        for key, value in request.headers.raw_items():
-            if key.lower() == "authorization":
-                token = value.replace("Bearer ", "")
-        # Also check query param ?token=
-        if not token and "token=" in (request.path or ""):
-            import urllib.parse
-            _, qs = request.path.split("?", 1) if "?" in request.path else (request.path, "")
-            params = urllib.parse.parse_qs(qs)
-            token = params.get("token", [None])[0]
-        if token != AUTH_TOKEN:
-            headers = Headers([("Content-Type", "text/plain")])
-            return Response(401, "Unauthorized", headers, b"Invalid token\n")
-
-    # Check if this is a WebSocket upgrade
-    upgrade = None
-    for key, value in request.headers.raw_items():
-        if key.lower() == "upgrade":
-            upgrade = value.lower()
+    headers = Headers([
+        ("Content-Type", content_type),
+        ("Cache-Control", "no-store"),
+        ("Content-Security-Policy", "default-src 'self'; connect-src 'self'; img-src 'self' data:; manifest-src 'self' data:; script-src 'unsafe-inline'; style-src 'unsafe-inline'; object-src 'none'; base-uri 'none'; frame-ancestors 'none'; form-action 'none'"),
+        ("Permissions-Policy", "camera=(), geolocation=(), microphone=(), payment=(), usb=()"),
+        ("Referrer-Policy", "no-referrer"),
+        ("X-Content-Type-Options", "nosniff"),
+        ("X-Frame-Options", "DENY"),
+    ])
+    return Response(status, reason, headers, body)
+
+
+async def process_request(connection, request):
+    """Serve the same-origin dashboard and gate WebSocket upgrades."""
+    path = (request.path or "/").split("?", 1)[0]
+    if path == "/healthz":
+        return http_response(200, "OK", b"ok\n", "text/plain; charset=utf-8")
+
+    if REQUIRE_ACCESS_JWT and not request_header(request, "Cf-Access-Jwt-Assertion"):
+        return http_response(401, "Unauthorized", b"Cloudflare Access JWT required\n", "text/plain; charset=utf-8")
+
+    upgrade = request_header(request, "Upgrade").lower()
     if upgrade == "websocket":
-        return None  # proceed with WebSocket handshake
-
-    # For CORS preflight
-    if request.path and "OPTIONS" in str(request.headers):
-        headers = Headers([
-            ("Access-Control-Allow-Origin", "*"),
-            ("Access-Control-Allow-Methods", "POST, OPTIONS"),
-            ("Access-Control-Allow-Headers", "Content-Type"),
-        ])
-        return Response(204, "No Content", headers, b"")
-
-    # Serve web app for GET / or GET /index.html
-    path = (request.path or "/").split("?")[0]
-    if path in ("/", "/index.html"):
-        web_dir = os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "web")
-        index_path = os.path.join(web_dir, "index.html")
-        if os.path.isfile(index_path):
-            with open(index_path, "rb") as f:
-                body = f.read()
-            headers = Headers([
-                ("Content-Type", "text/html; charset=utf-8"),
-                ("Cache-Control", "no-cache"),
-            ])
-            return Response(200, "OK", headers, body)
-
-    # Serve service worker
-    if path == "/sw.js":
-        web_dir = os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "web")
-        sw_path = os.path.join(web_dir, "sw.js")
-        if os.path.isfile(sw_path):
-            with open(sw_path, "rb") as f:
-                body = f.read()
-            headers = Headers([
-                ("Content-Type", "application/javascript"),
-                ("Cache-Control", "no-cache"),
-                ("Service-Worker-Allowed", "/"),
-            ])
-            return Response(200, "OK", headers, body)
-
-    # Serve VAPID public key
-    if path == "/api/vapid-public-key":
-        body = json.dumps({"publicKey": VAPID_PUBLIC_KEY}).encode()
-        headers = Headers([
-            ("Content-Type", "application/json"),
-            ("Access-Control-Allow-Origin", "*"),
-        ])
-        return Response(200, "OK", headers, body)
-
-    # Serve logo.svg
-    if path == "/logo.svg":
-        web_dir = os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "web")
-        svg_path = os.path.join(web_dir, "logo.svg")
-        if os.path.isfile(svg_path):
-            with open(svg_path, "rb") as f:
-                body = f.read()
-            headers = Headers([("Content-Type", "image/svg+xml")])
-            return Response(200, "OK", headers, body)
-
-    # HTTP POST — parse event from URL query params as fallback
-    import urllib.parse
-    if "?" in (request.path or ""):
-        _, qs = request.path.split("?", 1)
-        params = urllib.parse.parse_qs(qs)
-        if "d" in params:
-            try:
-                event = json.loads(urllib.parse.unquote(params["d"][0]))
-                event_queue.put_nowait(event)
-            except Exception:
-                pass
+        origin = request_header(request, "Origin")
+        if ALLOWED_ORIGIN and origin != ALLOWED_ORIGIN:
+            return http_response(403, "Forbidden", b"Invalid WebSocket origin\n", "text/plain; charset=utf-8")
+        if len(clients) >= MAX_CLIENTS:
+            return http_response(503, "Service Unavailable", b"Client limit reached\n", "text/plain; charset=utf-8")
+        return None
+
+    web_dir = os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "web")
+    static_files = {
+        "/": ("index.html", "text/html; charset=utf-8"),
+        "/index.html": ("index.html", "text/html; charset=utf-8"),
+        "/logo.svg": ("logo.svg", "image/svg+xml"),
+    }
+    static_file = static_files.get(path)
+    if static_file:
+        filename, content_type = static_file
+        file_path = os.path.join(web_dir, filename)
+        if os.path.isfile(file_path):
+            with open(file_path, "rb") as file:
+                return http_response(200, "OK", file.read(), content_type)
+
+    return http_response(404, "Not Found", b"Not found\n", "text/plain; charset=utf-8")
+
 
-    headers = Headers([("Access-Control-Allow-Origin", "*")])
-    return Response(200, "OK", headers, b"ok\n")
+def access_token_lifetime(request) -> int | None:
+    token = request_header(request, "Cf-Access-Jwt-Assertion")
+    if not token:
+        return None
+    try:
+        payload = token.split(".", 2)[1]
+        payload += "=" * (-len(payload) % 4)
+        claims = json.loads(base64.urlsafe_b64decode(payload))
+        return max(1, int(claims["exp"] - time.time()))
+    except (IndexError, KeyError, TypeError, ValueError, json.JSONDecodeError):
+        return None
+
+
+async def expire_client(ws, lifetime: int):
+    await asyncio.sleep(lifetime)
+    await ws.close(code=4001, reason="Cloudflare Access session expired")
 
 
 async def handle_client(ws):
@@ -452,6 +446,16 @@ async def handle_client(ws):
     log.info("Client connected: ip=%s device=%s origin=%s", ip, device, origin or "-")
     clients.add(ws)
     connected_at = time.monotonic()
+    access_lifetime = access_token_lifetime(ws.request)
+    connection_lifetime = MAX_CONNECTION_SECONDS
+    if access_lifetime is not None:
+        connection_lifetime = min(connection_lifetime, access_lifetime)
+    expiry_task = asyncio.create_task(expire_client(ws, connection_lifetime))
+    await ws.send(json.dumps({
+        "type": "relay_config",
+        "read_only": READ_ONLY,
+        "connection_lifetime_seconds": connection_lifetime,
+    }))
     try:
         async for raw in ws:
             try:
@@ -459,6 +463,10 @@ async def handle_client(ws):
             except json.JSONDecodeError:
                 continue
             msg_type = msg.get("type")
+            if READ_ONLY and msg_type in MUTATING_MESSAGE_TYPES:
+                audit("read_only_reject", ip, device, msg.get("pane_id", ""), f"type={msg_type}")
+                await ws.send(json.dumps({"type": "error", "message": "relay is read-only"}))
+                continue
             if msg_type == "respond":
                 pane_id = msg["pane_id"]
                 if pane_id not in known_panes:
@@ -544,6 +552,7 @@ async def handle_client(ws):
     except (ConnectionClosedError, ConnectionClosedOK):
         pass
     finally:
+        expiry_task.cancel()
         duration = int(time.monotonic() - connected_at)
         log.info("Client disconnected: ip=%s device=%s duration=%ds", ip, device, duration)
         clients.discard(ws)
@@ -558,6 +567,8 @@ class UDPPlugin(asyncio.DatagramProtocol):
 
 
 def start_mdns():
+    if not MDNS_ENABLED:
+        return None, None
     try:
         from zeroconf import Zeroconf, ServiceInfo
         import socket as sock_mod
@@ -585,10 +596,10 @@ async def main():
         log.warning("UDP 8376 in use, plugin push disabled")
     asyncio.create_task(poll_loop())
     asyncio.create_task(event_push())
-    server = await serve(handle_client, "0.0.0.0", WS_PORT, process_request=process_request)
+    server = await serve(handle_client, BIND_ADDRESS, WS_PORT, process_request=process_request)
     hosts = ["local"] + REMOTES
-    log.info("herdr-remote relay on :%d (WebSocket + HTTP POST)", WS_PORT)
-    log.info("Polling: %s", ", ".join(hosts))
+    log.info("herdr-remote relay on %s:%d", BIND_ADDRESS, WS_PORT)
+    log.info("Polling: %s; read_only=%s; access_jwt=%s", ", ".join(hosts), READ_ONLY, REQUIRE_ACCESS_JWT)
     stop = loop.create_future()
     for sig in (signal.SIGINT, signal.SIGTERM):
         loop.add_signal_handler(sig, stop.set_result, None)
diff --git a/web/index.html b/web/index.html
index c14df8a..49d2263 100644
--- a/web/index.html
+++ b/web/index.html
@@ -7,11 +7,6 @@
 <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
 <link rel="manifest" href="data:application/json,{&quot;name&quot;:&quot;herdr-remote&quot;,&quot;short_name&quot;:&quot;herdr&quot;,&quot;display&quot;:&quot;standalone&quot;,&quot;background_color&quot;:&quot;%23e1e2e7&quot;,&quot;theme_color&quot;:&quot;%23e1e2e7&quot;}">
 <title>herdr-remote</title>
-<script type="module">
-import { play, bind } from 'https://esm.sh/cuelume@0.1.2';
-window.cue = play;
-document.addEventListener('DOMContentLoaded', () => bind());
-</script>
 <style>
 :root {
   --bg: #e1e2e7; --surface: #d8d9e0; --border: #b6bbd1; --text: #1f2f66; --muted: #68709a;
@@ -52,6 +47,7 @@ body { font-family: -apple-system, system-ui, -webkit-system-font, sans-serif; b
 .setting-group button { margin-top: 8px; padding: 8px 16px; border-radius: 6px; border: none; background: var(--green); color: #000; font-weight: 600; font-size: 13px; cursor: pointer; }
 
 
+.read-only .write-control { display: none !important; }
 /* Agent list */
 .agents { padding: 12px; }
 .section-header { font-size: 0.7rem; font-weight: 600; letter-spacing: 0.05em; text-transform: uppercase; color: var(--muted); margin: 16px 0 8px; display: flex; align-items: center; gap: 5px; }
@@ -149,19 +145,9 @@ body { font-family: -apple-system, system-ui, -webkit-system-font, sans-serif; b
 <div class="settings" id="settingsView">
   <h2>Settings</h2>
   <div class="setting-group">
-    <label for="relayUrl">Relay URL</label>
-    <input id="relayUrl" type="url" placeholder="wss://your-tunnel.trycloudflare.com" />
-    <label for="relayToken" style="margin-top:8px">Token (optional)</label>
-    <input id="relayToken" type="password" placeholder="shared secret" />
-    <button onclick="saveAndConnect()">Connect</button>
-    <div class="hint">Run <code>./relay/start.sh</code> to get a URL. <a href="https://github.com/dcolinmorgan/herdr-remote/blob/main/QUICKSTART.md" style="color:var(--blue)">Setup guide →</a></div>
-  </div>
-
-  <div class="setting-group">
-    <label>Push Notifications</label>
-    <div id="pushStatus" style="font-size:0.75rem;margin-bottom:6px"></div>
-    <button id="pushToggle" onclick="togglePush()">Enable Push</button>
-    <div class="hint">Get notified when agents are blocked, even with this tab closed.</div>
+    <label>Security</label>
+    <div style="font-size:0.75rem">Same-origin Cloudflare Access session · read-only relay</div>
+    <div class="hint">Reloading this page performs the next Access authentication check.</div>
   </div>
   <div class="setting-group">
     <label>Status</label>
@@ -184,15 +170,15 @@ body { font-family: -apple-system, system-ui, -webkit-system-font, sans-serif; b
     <button class="refresh-btn" onclick="refreshPane()" aria-label="Refresh pane"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12a9 9 0 0 0-9-9 9.75 9.75 0 0 0-6.74 2.74L3 8"/><path d="M3 3v5h5"/><path d="M3 12a9 9 0 0 0 9 9 9.75 9.75 0 0 0 6.74-2.74L21 16"/><path d="M16 16h5v5"/></svg></button>
   </div>
   <div class="term-content" id="termContent">Loading…</div>
-  <div class="quick-actions" id="quickActions"></div>
-  <div class="term-input">
+  <div class="quick-actions write-control" id="quickActions"></div>
+  <div class="term-input write-control">
     <button onclick="openCommandPalette()" aria-label="Commands" style="padding:10px 12px;border-radius:6px;border:1px solid var(--blue);background:var(--bg);color:var(--blue);font-weight:700;font-size:14px;flex-shrink:0">/</button>
     <input id="termInput" placeholder="Type…" autocomplete="off" autocorrect="off" autocapitalize="off" spellcheck="false" />
     <button onclick="toggleQuickDock()" aria-label="Quick actions" style="padding:8px 10px;border-radius:6px;border:1px solid var(--border);background:var(--bg);color:var(--orange);flex-shrink:0"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M13 2 3 14h9l-1 8 10-12h-9l1-8z"/></svg></button>
     <button onclick="toggleKeysDock()" aria-label="Keys" style="padding:8px 10px;border-radius:6px;border:1px solid var(--border);background:var(--bg);color:var(--muted);flex-shrink:0"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M6 8h.001M10 8h.001M14 8h.001M18 8h.001M8 12h.001M12 12h.001M16 12h.001M7 16h10"/></svg></button>
     <button onclick="sendText()" aria-label="Send" style="padding:10px 14px;border-radius:6px;border:none;background:var(--blue);color:#fff;flex-shrink:0"><svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor" stroke="none"><path d="M2.01 21L23 12 2.01 3 2 10l15 2-15 2z"/></svg></button>
   </div>
-  <div class="term-keys" id="termKeys" style="display:none">
+  <div class="term-keys write-control" id="termKeys" style="display:none">
     <!-- Key queue strip (visible when composing) -->
     <div id="keyQueueStrip" style="display:none;width:100%;padding:6px 0;border-bottom:1px solid var(--border);margin-bottom:6px;flex-wrap:wrap;gap:4px;align-items:center"></div>
     <!-- Tab toggle -->
@@ -230,7 +216,7 @@ body { font-family: -apple-system, system-ui, -webkit-system-font, sans-serif; b
     <div id="actionKeys" style="width:100%;margin-top:4px"></div>
   </div>
   <!-- Quick actions dock -->
-  <div class="term-keys" id="quickDock" style="display:none">
+  <div class="term-keys write-control" id="quickDock" style="display:none">
     <div style="display:flex;align-items:center;justify-content:space-between;width:100%;margin-bottom:8px">
       <span style="font-size:0.7rem;font-weight:600;text-transform:uppercase;letter-spacing:0.05em;color:var(--muted)">Quick Actions</span>
       <button onclick="toggleQuickDock()" aria-label="Close" style="background:none;border:none;color:var(--muted);font-size:1rem;cursor:pointer">✕</button>
@@ -253,7 +239,7 @@ body { font-family: -apple-system, system-ui, -webkit-system-font, sans-serif; b
 </div>
 
 <!-- Command palette -->
-<div id="cmdPalette" style="display:none;position:fixed;inset:0;z-index:100;">
+<div class="write-control" id="cmdPalette" style="display:none;position:fixed;inset:0;z-index:100;">
   <div onclick="closePalette()" style="position:absolute;inset:0;background:rgba(0,0,0,0.5)"></div>
   <div style="position:absolute;bottom:0;left:50%;transform:translateX(-50%);width:100%;max-width:420px;max-height:50dvh;background:var(--surface);border-top-left-radius:16px;border-top-right-radius:16px;border-top:1px solid var(--border);display:flex;flex-direction:column;padding-bottom:env(safe-area-inset-bottom,8px)">
     <div style="padding:12px 16px;border-bottom:1px solid var(--border);display:flex;align-items:center;gap:8px">
@@ -271,18 +257,22 @@ let ws = null, agents = [], activePane = null, refreshInterval = null, userScrol
 let timeline = [], prevStatuses = {};
 let activeWorkspace = null;
 let activeTab = null;
+let readOnly = true;
+let reconnectFailures = 0;
 
-// Settings — auto-detect relay when served from same origin
 const autoRelayUrl = (location.protocol === 'https:' ? 'wss://' : 'ws://') + location.host;
-const DEMO_RELAY = 'wss://herdr-remote-demo.yyrzrh5wfg.workers.dev';
-const isDemo = location.hostname.includes('herdr-demo.pages.dev');
-const isSelfRelay = !isDemo && !location.hostname.includes('pages.dev') && !location.hostname.includes('localhost');
-const savedUrl = localStorage.getItem('herdr_relay_url') || (isSelfRelay ? autoRelayUrl : (isDemo ? DEMO_RELAY : ''));
-const urlToken = new URLSearchParams(location.search).get('token');
-if (urlToken) localStorage.setItem('herdr_relay_token', urlToken);
-const savedToken = localStorage.getItem('herdr_relay_token') || '';
-document.getElementById('relayUrl').value = savedUrl;
-document.getElementById('relayToken').value = savedToken;
+const reauthReloadKey = 'herdr_reauth_reload_attempted';
+document.body.classList.add('read-only');
+
+function escapeHtml(value) {
+  return String(value ?? '').replace(/[&<>"']/g, char => ({
+    '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;'
+  })[char]);
+}
+
+function safeIdentifier(value) {
+  return String(value ?? '').replace(/[^A-Za-z0-9:_-]/g, '');
+}
 
 
 
@@ -293,9 +283,9 @@ function toggleSettings() {
   document.getElementById('agentListView').style.display = show ? 'none' : '';
   document.getElementById('timelineView').style.display = 'none';
   if (show) {
-    document.getElementById('settingsStatus').innerHTML = ws && ws.readyState === 1
-      ? `<span style="color:var(--green)">● Connected</span> · ${agents.length} agents`
-      : `<span style="color:var(--red)">● Disconnected</span>`;
+    document.getElementById('settingsStatus').textContent = ws && ws.readyState === 1
+      ? `Connected · ${agents.length} agents`
+      : 'Disconnected';
   }
 }
 
@@ -314,36 +304,43 @@ function renderTimeline() {
   el.innerHTML = timeline.map(e => {
     const t = e.time.toLocaleTimeString([], {hour:'2-digit', minute:'2-digit'});
     const color = e.status==='blocked'?'var(--red)':e.status==='working'?'var(--green)':'var(--muted)';
-    return `<div style="display:flex;gap:8px;padding:4px 0;border-bottom:1px solid var(--border)"><span style="color:var(--muted)">${t}</span><span style="flex:1">${e.project} (${e.agent})</span><span style="color:${color}">${e.status}</span></div>`;
+    return `<div style="display:flex;gap:8px;padding:4px 0;border-bottom:1px solid var(--border)"><span style="color:var(--muted)">${escapeHtml(t)}</span><span style="flex:1">${escapeHtml(e.project)} (${escapeHtml(e.agent)})</span><span style="color:${color}">${escapeHtml(e.status)}</span></div>`;
   }).join('');
 }
 
-function saveAndConnect() {
-  const url = document.getElementById('relayUrl').value.trim();
-  if (!url) return;
-  localStorage.setItem('herdr_relay_url', url);
-  const token = document.getElementById('relayToken').value.trim();
-  if (token) localStorage.setItem('herdr_relay_token', token);
-  else localStorage.removeItem('herdr_relay_token');
-  connect();
-  toggleSettings();
-}
 
 // Connection
 function connect() {
-  let url = localStorage.getItem('herdr_relay_url') || (isSelfRelay ? autoRelayUrl : (isDemo ? DEMO_RELAY : ''));
-  if (!url) { showSetup(); return; }
   if (ws) ws.close();
   setStatus('connecting');
-  // Append token as query param if stored separately
-  const token = localStorage.getItem('herdr_relay_token');
-  let wsUrl = url;
-  if (token) wsUrl += (url.includes('?') ? '&' : '?') + 'token=' + encodeURIComponent(token);
-  ws = new WebSocket(wsUrl);
-  ws.onopen = () => { setStatus('connected'); if(window.cue) cue('ready'); };
-  ws.onclose = () => { setStatus('disconnected'); setTimeout(connect, 3000); };
+  ws = new WebSocket(autoRelayUrl);
+  ws.onopen = () => {
+    reconnectFailures = 0;
+    sessionStorage.removeItem(reauthReloadKey);
+    setStatus('connected');
+  };
+  ws.onclose = event => {
+    setStatus('disconnected');
+    if (event.code === 4001) {
+      sessionStorage.setItem(reauthReloadKey, '1');
+      location.reload();
+      return;
+    }
+    reconnectFailures += 1;
+    if (reconnectFailures >= 3) {
+      showSetup();
+      if (!sessionStorage.getItem(reauthReloadKey)) {
+        sessionStorage.setItem(reauthReloadKey, '1');
+        setTimeout(() => location.reload(), 1000);
+        return;
+      }
+      setTimeout(connect, 15000);
+      return;
+    }
+    setTimeout(connect, 3000);
+  };
   ws.onerror = () => setStatus('disconnected');
-  ws.onmessage = (e) => handleMessage(JSON.parse(e.data));
+  ws.onmessage = event => handleMessage(JSON.parse(event.data));
 }
 
 function setStatus(s) {
@@ -356,25 +353,17 @@ function setStatus(s) {
 
 function showSetup() {
   document.getElementById('agents').innerHTML = `<div class="empty">
-    <p style="font-size:1.5rem;margin-bottom:12px">🐑</p>
-    <p><strong>herdr-remote</strong></p>
-    <p style="margin-top:8px">Monitor & approve agents from your phone</p>
-    <p style="margin-top:16px"><button onclick="tryDemo()" style="padding:10px 20px;border-radius:8px;border:none;background:var(--blue);color:#fff;font-weight:600;font-size:0.85rem;cursor:pointer">Try Demo</button></p>
-    <p style="margin-top:16px;font-size:0.75rem;color:var(--muted);text-align:left;max-width:280px;margin-left:auto;margin-right:auto">
-      <strong>Connect your own:</strong><br>
-      1. Run: <code style="font-size:0.7rem">cd relay && ./start.sh</code><br>
-      2. Tap ⚙ → paste the wss:// URL<br>
-    </p>
+    <p><strong>Herdr Remote is offline</strong></p>
+    <p style="margin-top:8px">The same-origin relay is unavailable.</p>
   </div>`;
 }
-function tryDemo() {
-  localStorage.setItem('herdr_relay_url', 'wss://herdr-remote-demo.yyrzrh5wfg.workers.dev');
-  localStorage.removeItem('herdr_relay_token');
-  document.getElementById('relayUrl').value = 'wss://herdr-remote-demo.yyrzrh5wfg.workers.dev';
-  connect();
-}
 
 function handleMessage(msg) {
+  if (msg.type === 'relay_config') {
+    readOnly = msg.read_only !== false;
+    document.body.classList.toggle('read-only', readOnly);
+    return;
+  }
   if (msg.type === 'agents') {
     // Track timeline on status changes
     for (const a of msg.agents) {
@@ -451,7 +440,8 @@ function renderWorkspaces(workspaces) {
     const wsAgents = agents.filter(a => a.workspace_id === wsId);
     const hasBlocked = wsAgents.some(a => a.status === 'blocked');
     const name = wsAgents[0]?.project || wsId.slice(0, 8);
-    html += `<button class="chip${activeWorkspace===wsId?' active':''}${hasBlocked?' alert':''}" onclick="selectWorkspace('${wsId}')">${name}</button>`;
+    const safeWsId = safeIdentifier(wsId);
+    html += `<button class="chip${activeWorkspace===wsId?' active':''}${hasBlocked?' alert':''}" onclick="selectWorkspace('${safeWsId}')">${escapeHtml(name)}</button>`;
   }
   html += `</div>`;
   // Tab chip strip (always show when workspace selected)
@@ -465,9 +455,9 @@ function renderWorkspaces(workspaces) {
       const tabAgents = agents.filter(a => a.tab_id === tid);
       const hasBlocked = tabAgents.some(a => a.status === 'blocked');
       const tabLabel = tabAgents[0]?.label || 'Tab '+(i+1);
-      html += `<button class="chip${activeTab===tid?' active':''}${hasBlocked?' alert':''}" onclick="selectTab('${tid}')">${tabLabel}</button>`;
+      html += `<button class="chip${activeTab===tid?' active':''}${hasBlocked?' alert':''}" onclick="selectTab('${safeIdentifier(tid)}')">${escapeHtml(tabLabel)}</button>`;
     });
-    html += `<button class="chip chip-add" onclick="createTab('${activeWorkspace}')">+</button>`;
+    if (!readOnly) html += `<button class="chip chip-add write-control" onclick="createTab('${safeIdentifier(activeWorkspace)}')">+</button>`;
     html += `</div>`;
   }
   // Agent list (filtered)
@@ -513,12 +503,13 @@ function createTab(workspaceId) {
 function agentCard(a) {
   const color = a.status==='blocked'?'var(--red)':a.status==='working'?'var(--green)':'var(--muted)';
   const pulseClass = a.status==='working'?' pulse':'';
-  const host = a.host&&a.host!=='local'?` <span style="color:var(--orange);font-size:0.6rem">@${a.host}</span>`:'';
-  const cwd = a.cwd ? `<span style="font-family:monospace;opacity:0.7">${a.cwd.split('/').slice(-2).join('/')}</span>` : '';
+  const host = a.host&&a.host!=='local'?` <span style="color:var(--orange);font-size:0.6rem">@${escapeHtml(a.host)}</span>`:'';
+  const cwd = a.cwd ? `<span style="font-family:monospace;opacity:0.7">${escapeHtml(a.cwd.split('/').slice(-2).join('/'))}</span>` : '';
   const label = a.label||a.project||a.agent;
-  return `<div class="agent${a.status==='blocked'?' blocked':''}" role="button" tabindex="0" aria-label="${label}, ${a.status}" onclick="openTerminal('${a.pane_id}')" onkeydown="if(event.key==='Enter'||event.key===' '){event.preventDefault();openTerminal('${a.pane_id}')}">
+  const paneId = safeIdentifier(a.pane_id);
+  return `<div class="agent${a.status==='blocked'?' blocked':''}" role="button" tabindex="0" aria-label="${escapeHtml(label)}, ${escapeHtml(a.status)}" onclick="openTerminal('${paneId}')" onkeydown="if(event.key==='Enter'||event.key===' '){event.preventDefault();openTerminal('${paneId}')}">
     <span class="dot${pulseClass}" style="background:${color}" aria-hidden="true"></span>
-    <div class="info"><div class="project">${label}${host}</div><div class="meta">${a.agent} · ${cwd}</div></div>
+    <div class="info"><div class="project">${escapeHtml(label)}${host}</div><div class="meta">${escapeHtml(a.agent)} · ${cwd}</div></div>
     <span style="color:var(--muted);font-size:1.2rem" aria-hidden="true">›</span>
   </div>`;
 }
@@ -536,7 +527,7 @@ function openTerminal(paneId) {
   document.getElementById('terminalView').classList.add('active');
   const qa = document.getElementById('quickActions');
   const ak = document.getElementById('actionKeys');
-  if (a&&a.status==='blocked') {
+  if (!readOnly && a&&a.status==='blocked') {
     const opts = a.options||['yes, single permission','trust, always allow','no (tab to edit)'];
     qa.innerHTML = opts.map(o => {
       const cls = o.includes('yes')||o.includes('approve')?'btn-yes':o.includes('trust')?'btn-trust':'btn-no';
@@ -721,7 +712,7 @@ document.getElementById('termContent').addEventListener('scroll', function() {
   if (el.scrollTop === 0 && paneLines < 5000) loadMore();
 });
 
-if (savedUrl) setTimeout(connect, 100); else showSetup();
+setTimeout(connect, 100);
 
 // --- Command Palette ---
 const COMMANDS = {
@@ -808,83 +799,6 @@ function runCommand(cmd) {
   setTimeout(refreshPane, 500);
 }
 
-// --- Web Push ---
-let pushSubscription = null;
-
-async function initPush() {
-  if (!('serviceWorker' in navigator) || !('PushManager' in window)) {
-    document.getElementById('pushStatus').textContent = 'Push not supported in this browser';
-    document.getElementById('pushToggle').style.display = 'none';
-    return;
-  }
-  try {
-    const reg = await navigator.serviceWorker.register('/sw.js');
-    pushSubscription = await reg.pushManager.getSubscription();
-    updatePushUI();
-  } catch (e) {
-    document.getElementById('pushStatus').textContent = 'Service worker error: ' + e.message;
-  }
-}
-
-function updatePushUI() {
-  const btn = document.getElementById('pushToggle');
-  const status = document.getElementById('pushStatus');
-  if (pushSubscription) {
-    btn.textContent = 'Disable Push';
-    btn.style.background = 'var(--red)';
-    status.innerHTML = '<span style="color:var(--green)">● Enabled</span>';
-  } else {
-    btn.textContent = 'Enable Push';
-    btn.style.background = 'var(--green)';
-    status.innerHTML = '<span style="color:var(--muted)">○ Disabled</span>';
-  }
-}
-
-async function togglePush() {
-  if (pushSubscription) {
-    // Unsubscribe
-    if (ws && ws.readyState === 1) {
-      ws.send(JSON.stringify({type: 'push_unsubscribe', subscription: pushSubscription.toJSON()}));
-    }
-    await pushSubscription.unsubscribe();
-    pushSubscription = null;
-    updatePushUI();
-  } else {
-    // Subscribe
-    try {
-      const relayUrl = localStorage.getItem('herdr_relay_url') || '';
-      const httpUrl = relayUrl.replace('wss://', 'https://').replace('ws://', 'http://');
-      const resp = await fetch(httpUrl + '/api/vapid-public-key');
-      const {publicKey} = await resp.json();
-      if (!publicKey) {
-        document.getElementById('pushStatus').textContent = 'VAPID key not configured on relay';
-        return;
-      }
-      const reg = await navigator.serviceWorker.ready;
-      pushSubscription = await reg.pushManager.subscribe({
-        userVisibleOnly: true,
-        applicationServerKey: urlBase64ToUint8Array(publicKey)
-      });
-      if (ws && ws.readyState === 1) {
-        ws.send(JSON.stringify({type: 'push_subscribe', subscription: pushSubscription.toJSON()}));
-      }
-      updatePushUI();
-    } catch (e) {
-      document.getElementById('pushStatus').textContent = 'Error: ' + e.message;
-    }
-  }
-}
-
-function urlBase64ToUint8Array(base64String) {
-  const padding = '='.repeat((4 - base64String.length % 4) % 4);
-  const base64 = (base64String + padding).replace(/-/g, '+').replace(/_/g, '/');
-  const raw = atob(base64);
-  const arr = new Uint8Array(raw.length);
-  for (let i = 0; i < raw.length; i++) arr[i] = raw.charCodeAt(i);
-  return arr;
-}
-
-initPush();
 </script>
 </body>
 </html>
